import { describe, expect, it, screen } from "vitest"
// import { render, screen } from "@testing-library"
import App from "./App"

describe("un test", () => {
  it("deberia mostrar el boton e input para insertar tarea", () => {
    // render(<Atask />)
    // const findButton =
    // expect(screen.getByRole(input, { name: "new task" })).toBeInThejestDocument();
  })
})